{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Ming Salvage Web",
      "type": "shell",
      "command": "npm install && npm run build",
      "options": {
        "cwd": "${workspaceFolder}/web"
      },
      "problemMatcher": "$tsc",
      "group": "build",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
